# TODO write tests, only tested manually
# Start an agent with each nagios example and telnet it (no text required)
# should return weight in percent:
#        default: 100%
#        critical: 0%
#        85: 25% (half of drop to 50%)
